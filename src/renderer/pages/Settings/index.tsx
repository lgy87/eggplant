import React, { FC, memo } from "react"
import Page from "~/components/Page"
import { EmptyObject } from "~/global"

const Settings: FC<EmptyObject> = () => {
  return (
    <Page>
      <h1>2234</h1>
    </Page>
  )
}

export default memo(Settings)
